# Add near the top of your Streamlit app.py
import os

# Add near login validation block
FLASK_BASE_URL = os.getenv("FLASK_BASE_URL", "http://127.0.0.1:5000")

# Replace your requests.get validate-token call with:
resp = requests.get(
    f"{FLASK_BASE_URL}/validate-token",
    params={"token": token},
    timeout=5,
)

# Replace your logout redirect URL with:
st.markdown(
    f'<meta http-equiv="refresh" content="0; url={FLASK_BASE_URL}/logout">',
    unsafe_allow_html=True
)
